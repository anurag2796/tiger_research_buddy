FROM qwen2.5:32b

# Set parameters
# Removed num_ctx and temperature to let the client control context dynamically and strictly enforce low factual temperatures.

# System persona is handled dynamically at runtime by the PromptBuilder/Synthesizer.

# Template for chat (Standard Llama 3 format)
TEMPLATE """{{ if .System }}<|start_header_id|>system<|end_header_id|>

{{ .System }}<|eot_id|>{{ end }}{{ if .Prompt }}<|start_header_id|>user<|end_header_id|>

{{ .Prompt }}<|eot_id|>{{ end }}<|start_header_id|>assistant<|end_header_id|>

{{ .Response }}<|eot_id|>"""
