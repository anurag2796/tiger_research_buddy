# Convergence Triage — 121 findings -> 23 canonical issues

This confirms D5 directly resolves the multi-region consensus tension. I have everything needed. Producing the deliverable.

---

# Triage: 121 Critical+High Findings → Canonical Clusters

The 121 findings (36 critical, 85 high) across 4 adversarial rounds dedup to **23 canonical issues**. Each is classified against the locked decisions D1–D7.

## Clustered Table

| # | Cluster (recurring finding IDs) | Classification | D# / Fix | Note |
|---|---|---|---|---|
| C1 | **Wedge is commoditized secure-RAG** — Phase-0 MVP = single-lab grounded Q&A, no moat, head-on with Glean/NotebookLM/ChatGPT-Ent (20, 48, 49, 79, 109, 118) | RESOLVED-BY-DECISION (partial) | D1 + D2 reframe the moat as cross-institution team-assembly + confidential proposal workspace + federation, NOT the DB/RAG. **Residual FIXABLE-GAP:** Phase-0 must carry one federation-flavored differentiator (cross-institution public-tier expert discovery on OpenAlex — zero confidentiality machinery) so first-dollar isn't pure commodity (per fix 118). | D1 kills the "commodity arena" critique at the positioning level; the gap is ensuring the *built* Phase-0 reflects D2's decomposition, not a generic appliance. |
| C2 | **Cold-start / network effect unsolved** — moat worthless at N=1, marginal at N=2; LOIs ≠ deployed nodes (23, 52, 110, 113) | RESOLVED-BY-DECISION | D3: anchor on ONE existing federally-funded multi-site center (NIH U54/U01, NSF AI Institute) with day-one N≥2 + existing DUA + recurring funded grant need. This is exactly fixes 23/110's "find an organic unit already sharing confidential data under a legal vehicle." | Plan MUST name the anchor-center selection criteria as a gate, not aspiration. |
| C3 | **Beachhead buyer unconfirmed / four-way AND** — pre-bonded dyad + executable master agreement + co-deploy + budget (48, 56, 87, 23) | RESOLVED-BY-DECISION | D3 collapses the four-way AND: the anchor center already has the legal basis (DUA), N≥2, and funded need. Buyer identity also resolved by D1 (RD/center-admin office). | Removes the "rare artifact" risk; replaced by "select the right existing center." |
| C4 | **Inverted unit economics** — $179–499/mo or $30k/yr ACV below GPU+idle COGS ($36–60k/yr) (21, 26, 51, 89, 111, 115) | RESOLVED-BY-DECISION | D7: institutional/center ACV ≥ 2–3× per-tenant COGS is the revenue anchor; $179/mo individual is explicitly top-of-funnel (loss-leader), not the margin driver. Directly answers fixes 111/115 "raise kill-gate ASP / re-anchor who the buyer is." | Plan MUST state the kill-gate floor ASP at ≥2–3× modeled per-tenant COGS. |
| C5 | **Multi-tenancy isolation cost vs cheap PLG** — dedicated GPU cell can't serve at PLG price (26, 60, 21) | RESOLVED-BY-DECISION | D7: non-confidential = multi-tenant **pooled**; dedicated isolation reserved for the confidential tier only. PLG runs on pooled infra; isolation cost lands only on the priced confidential institutional tier. | Removes the "every tenant needs a dedicated GPU" cost assumption. |
| C6 | **Revocation leak window in central index** — stale metadata/embedding persists after revoke/reclassify; CDC tombstone path unspecified (0, 9, 37, 40, 66, 72, 94, 102) | RESOLVED-BY-DECISION (partial) | D5: owning node is SOLE local fail-closed authority for access/revocation; D6: confidential content never enters the shared index. Confidential revocation is enforced at the owning node, not via the eventually-consistent index. **Residual FIXABLE-GAPs below (G-rev).** | D5/D6 remove the *confidential* leak vector (it was never in the index); residual gaps are about *security-reason* revocation and tombstone ordering for shared metadata. |
| C7 | **No-global-consensus / strongly-consistent-revocation-set CAP tension** — replicated set can't be strongly consistent at consumer; multi-region authority on hot path (1, 31, 32, 34, 61, 62, 92, 93) | RESOLVED-BY-DECISION | D5: **NO global multi-region consensus on the hot path.** Owning institution node is the sole local authority; discovery metadata eventually-consistent, confidentiality decisions strongly consistent **at the owning node only.** This is precisely the option fixes 31/62/92 demanded. | The single biggest resolved cluster. Plan MUST NOT reintroduce a global linearizable revocation authority. |
| C8 | **Confidential content / embeddings in shared index** — membership inference, embedding inversion, LSH non-invertibility asserted not proven (8, 13, 38, 42, 67, 98) | RESOLVED-BY-DECISION | D6: confidential content NEVER enters the shared central index (public + explicitly-shared metadata + non-reversible signals only); membership-inference/embedding-inversion red-team is a **GATE on synthetic data BEFORE any shared-index write.** Directly adopts fixes 98/67 ("prove the bound on synthetic corpora before any real write," "move red-team to a pre-GA gate"). | Plan MUST run the red-team as a pre-write gate, not Phase-2 GA gate. |
| C9 | **Classifier single trust root / fail-open / no adjudication** (6, 41, 97) | RESOLVED-BY-DECISION | D6: classifier abstention → quarantine **default-deny** (treated confidential, excluded from retrieval) + human adjudication. Resolves fix 97's "define abstention semantics, quarantine to unclassified=confidential." | Plan MUST ship quarantine+adjudication in the FIRST phase that touches classification, not deferred. |
| C10 | **Confidentiality enforcement makes "pluggable module" unachievable** — 7 mechanisms every module must reimplement (15, 16, 44, 74, 78, 95, 103, 104) | RESOLVED-BY-DECISION | D4: SINGLE Policy Enforcement Point (PEP) + data-access broker chokepoint; modules stay pluggable behind it and NEVER re-implement enforcement. Exactly fix 103's "mediated chokepoint, not a contract every module reimplements." | Plan MUST route ALL retrieval/egress/derivation through the one PEP + broker. |
| C11 | **Outbox is trusted not checked** — no independent egress validator (7, 19, 46) | RESOLVED-BY-DECISION | D4 (the PEP IS the egress chokepoint) + D6 (publishable-only invariant). The independent deny-by-default egress filter (fix 7) is the PEP. | The egress PEP must re-evaluate classification against an allowlist schema at the boundary. |

| C12 | **Operator in TCB / managed-cell ≠ zero-trust but sold as confidential** (12, 39, 71, 99) | ACCEPTED-TRADEOFF | D7 reserves dedicated isolation for the confidential tier but does NOT promise operator-zero-trust. **Document:** managed-cell = vendor-operated isolation + per-tenant keys + egress PEP; vendor is a *processor with technical+contractual controls*, NOT operator-zero-trust until TEE/HYOK sovereign. Plaintext-at-use exposure in cell memory must be stated honestly; no-retention on BYO endpoints is **contractual**, not cryptographically enforceable. |
| C13 | **Brokered drill-down availability is a product, not a min** — A-PEP × token-mint × B-cell ≈ 98.5%, compounds (3, 65) | ACCEPTED-TRADEOFF | Inherent to D5 (owning node is sole authority → cross-node op availability is multiplicative). **Document:** separate SLOs for index-path vs brokered-path; explicit degraded mode ("owning node unreachable → publishable metadata only, detail unavailable"); state honest compound availability of confidential cross-institution drill-down under control-plane degradation. |
| C14 | **Cross-region drill-down latency unbudgeted** (36, 92) | ACCEPTED-TRADEOFF (mostly resolved) | D5 removes the cross-region *consensus* cost (local authority + lease-based local read). **Document:** the residual worst-case latency table (US subject → EU confidential data → generated answer): cross-region RTT + local Check + local revocation read + vLLM TTFT + generation, reconciled against the Q&A SLO. |
| C15 | **Deemed-export / nationality attribute unverifiable via SSO** (11, 43, 100) | FIXABLE-GAP | Federated IdP does not carry verified citizenship. **Fix:** gate export-controlled access by an explicit institution-attested US-person determination recorded as a signed grantor caveat (export-control-officer assertion), NOT an SSO-carried attribute. Make owner-side authoritative re-derivation of grant scope (grantee attributes = untrusted hints) the **universal** invariant for ALL cross-tenant access (generalizes fix 100). Caveats must be sticky and re-evaluated at grantee-side access. |
| C16 | **ABAC+ReBAC composition / PIP fail-mode** (10) | FIXABLE-GAP | **Fix:** indeterminate/missing ABAC attribute → DENY for confidential+export tiers; ABAC may only NARROW a ReBAC grant never widen (tested invariant); PIP unavailability on a confidential check → deny, no cache-fallback. Lives inside the single PEP (D4). |
| C17 | **Audit divergence has no defined response; operator can rewrite own chain** (69, 101) | FIXABLE-GAP | **Fix:** (a) fair-exchange — owner issues signed access-receipt binding (grant_id, version, scope, jti, watermark, ts) and gets a signed request-receipt BEFORE serving bytes; define divergence response (freeze grant / P1 / suspend pending investigation). (b) Anchor every node's audit chain to an external operator-independent commitment (periodic signed chain-head checkpoints to exchange/control plane and/or RFC-3161 TSA / transparency log) so internal-only access is tamper-evident even against a compelled operator. |
| C18 | **Subject deprovisioning only checked for confidential drill-down** (68) | FIXABLE-GAP | **Fix:** extend subject-not-deprovisioned freshness check to ALL non-public tiers and to the discovery PEP; make subject-revocation a first-class check in the standard PEP decision flow on every request touching private/confidential. Compatible with D4 (one PEP) + D5 (checked at owning node). |
| C19 | **Security-reason revocation must not use the benign allow-window** (102) — residual of C6 | FIXABLE-GAP | **Fix:** distinguish revocation REASON, not just tier. Any revocation flagged security / consent-withdrawal / compromise MUST take the synchronous deny-correct path regardless of tier. The bounded allow-window applies ONLY to benign administrative revocations of low-sensitivity data. |
| C20 | **GDPR/FERPA erasure not satisfied by crypto-shred** — central index holds projections under control-plane keys (40) | FIXABLE-GAP | **Fix:** per-data-subject erasure workflow distinct from tenant-KEK shred: targeted hard-delete + tombstone of that subject's projections/embeddings in the shared index (reuse revocation-tombstone path keyed by subject), plus Art. 19 recipient-notification to consumers that pulled the projection. (Smaller surface now that D6 keeps confidential out of the index, but published projections still need it.) |
| C21 | **Central index growth/cost unmodeled in dollars; AGE multi-hop unbenchmarked; topical-shard skew/drift** (2, 4, 5, 35, 59, 63, 96) | FIXABLE-GAP | **Fix:** put a real growth model in the plan body (corpus per node tier → embedding bytes, graph node/edge cardinality → store size, recurring re-embedding cost) at N=10/50/200; assign the central index a dollar line + managed-vs-self-managed ops decision; benchmark AGE multi-hop + PPR at realistic R1 scale with a concrete fallback switch criterion (Neo4j/Memgraph); cost topical-shard skew + re-clustering migration on synthetic OpenAlex. Decouple lease-liveness from re-write (lightweight TTL index, not full re-publish). Add per-tenant noisy-neighbor quotas/fair-scheduling on shared-index write/query and shared-cell GPU/RAM. |
| C22 | **Phase-0 scope too large for 3 engineers; Phase-1 is a multi-year program; over-built eventing** (25, 29, 30, 55, 85, 116, 117) | FIXABLE-GAP | **Fix:** ruthlessly separate walking-skeleton MVP from platform investment. Phase-0: thin PEP over library RBAC (NOT full SpiceDB+Cedar), append-only Postgres audit (defer merkle/hash-chain), classification column + if/else router, Dagster ONLY (defer Temporal+Debezium+Kafka+schema-registry; use transactional-outbox-polling). Attach engineer-weeks per Phase-0 item + a calendar duration. Decompose Phase-1 into ≥3 shippable sub-phases each with its own revenue/proof gate (e.g. 1.0 = public-tier 2-node discovery, NO confidential grants / no revocation authority / no SpiceDB replication). |
| C23 | **Compliance/SOC2/runway costs missing from burn model** — SOC2 Type II 6–12mo window, salaries omitted, audit cash not lined (28, 54, 86, 120, 84, 114, 82) | FIXABLE-GAP | **Fix:** add a real monthly-burn model (5 FTE loaded + fractional GRC/counsel + infra COGS + tooling + audit fees as a discrete line) and overlay first-dollar / first-recurring-revenue dates against the security-review + SOC2 reality. Sequence SOC2 Type I (point-in-time) first; validate the "sandbox/de-identified pilot avoids full security review" claim in writing with a named buyer's CISO/contracts officer as a Week-1 kill-gate sub-condition. Reconcile the race-window thesis (lock ≥2 consortia) against the real Phase-3 timeline — sign multi-year framework LOIs early or drop the race-window as the defensibility claim. |

**Maintainability sub-cluster note:** findings 14, 17, 18, 44, 45, 46, 47, 74, 75, 76, 77, 78, 95, 104, 105, 106, 107, 108 split between C10 (D4 resolves the enforcement-reimplementation core) and C22 (scope). The *residual* maintainability gaps that D4 does NOT auto-resolve are folded into the checklist below (G13–G17).

---

# MUST-IMPLEMENT CHECKLIST

This is the contract the rebuilt plan is verified against. **FIXABLE-GAP fixes (must be implemented)** and **ACCEPTED-TRADEOFF documentation items (must be explicitly written down)** only — items fully resolved by D1–D7 with no residual are omitted.

### A. Confidentiality enforcement (residual gaps behind D4/D5/D6)
1. **(C15)** Gate export-controlled access by an explicit institution-attested **US-person signed grantor caveat** (export-control-officer assertion), never an SSO-carried nationality attribute. Make **owner-side authoritative re-derivation of grant scope the universal invariant for ALL cross-tenant access** (grantee/broker assertions are untrusted hints). Caveats are sticky and re-evaluated at grantee-side access (transfer_legality, export_attestation, FERPA role).
2. **(C16)** In the single PEP: missing/indeterminate ABAC attribute → DENY for confidential+export tiers; ABAC may only NARROW a ReBAC grant, never widen (tested invariant); PIP unavailability on a confidential check → deny, no cache-fallback.
3. **(C18)** Extend the subject-not-deprovisioned freshness check to ALL non-public tiers AND the discovery PEP; make subject-revocation a first-class check in the standard PEP decision flow on every private/confidential-touching request.
4. **(C19)** Revocation carries a REASON; any security / consent-withdrawal / compromise revocation takes the synchronous deny-correct path regardless of tier. The bounded allow-window applies only to benign administrative revocations of low-sensitivity data.
5. **(C20)** Implement a per-data-subject GDPR/FERPA erasure workflow distinct from tenant-KEK crypto-shred: targeted hard-delete + tombstone of the subject's published projections/embeddings in the shared index (subject-keyed reuse of the revocation-tombstone path) + Art. 19 recipient-notification to consumers that pulled them.
6. **(C17a)** Fair-exchange audit: owner issues a signed access-receipt binding (grant_id, grant_version, scope, token_jti, watermark, ts) and obtains a signed request-receipt BEFORE serving bytes; define the divergence response (freeze grant / P1 / suspend pending investigation) — detection alone is not a control.
7. **(C17b)** Anchor every node's audit chain to an external operator-independent commitment on a cadence (signed chain-head checkpoints to exchange/control plane and/or RFC-3161 TSA / transparency log) so internal-only access is tamper-evident even against a compelled node operator.
8. **(C8/C9 enforcement timing)** Run the membership-inference + embedding-inversion + cross-tenant-linkage red-team on **synthetic/de-identified corpora as a gate BEFORE any real shared-index write** (per D6) — not a Phase-2 GA gate. Ship classifier-abstention quarantine (unclassified = confidential, excluded from all retrieval) + human adjudication in the first phase that touches classification.

### B. Distributed systems / scale (FIXABLE-GAP)
9. **(C21a)** Put a real growth + dollar model for the central index in the plan body at N=10/50/200 (embedding count×dim→bytes, graph cardinality→store size, recurring re-embedding cost); choose managed-vs-self-managed ops and assign an owner.
10. **(C21b)** Benchmark Apache AGE multi-hop + HippoRAG2 PPR at realistic R1 graph scale before locking it; define a concrete fallback switch criterion to Neo4j/Memgraph.
11. **(C21c)** Cost topical-shard skew (power-law topics) + the re-clustering/re-embedding migration on synthetic OpenAlex; do not assert "few shards per query."
12. **(C21d)** Decouple lease-liveness from full re-publish (lightweight per-record-version TTL index). Add per-tenant noisy-neighbor quotas + fair-scheduling on shared-index write/query and shared-cell GPU/RAM.

### C. Maintainability (residual behind D4)
13. **(C10 residual)** Make the tier set a **tiny near-frozen formally-specified lattice (3 tiers + ordering)** owned in shared/contracts; push per-feature classification CODES to an extensible registry. Do NOT claim compiler-enforced exhaustiveness across distributed consumers — use a versioned artifact + per-consumer conformance registry + min-node-version floor where unknown tiers are treated MOST-restrictive (safe-by-construction, not blocked-by-barrier).
14. **(C10 residual)** Single owned tier→locality routing-policy table that BOTH the router and the transport/egress layer consult — not two independent re-derivations. Define `IModelProvider` (capabilities, locality-class satisfied, cost, no-train attestation, embedding dim); router selects over a registry of providers declaring their locality class.
15. **(C13/maint)** Define module-to-module communication as versioned domain EVENTS (registry + Avro/Protobuf + compatibility rules) from Phase-0 even inside the monolith (in-process bus, same contract), so the extraction escape-hatch actually exists.
16. **(maint)** Enforce per-module Postgres schemas + per-module DB roles with REVOKE on other modules' schemas from **Phase-0** (DDL+GRANT, nearly free); import-linter alone cannot stop shared-table reads.
17. **(maint)** Specify consumer-driven contract tests (Pact-style) between every cell and the exchange as a CI gate; make the TierLattice MAX-rule, sticky-flag UNION, and source-class re-derivation property-tested invariants. State the honest MVP posture as a **modular monolith** (single deployable, in-process boundaries), not "independently-deployed services."

### D. Scope / phasing / economics (FIXABLE-GAP)
18. **(C22)** Cut Phase-0 to a walking skeleton: thin PEP over library RBAC, append-only Postgres audit (defer hash-chain/merkle), classification column + if/else router, Dagster only. Defer Temporal + Debezium + Kafka + schema-registry; use transactional-outbox-polling. Attach engineer-weeks per Phase-0 item and a calendar duration.
19. **(C22)** Decompose Phase-1 into ≥3 shippable sub-phases each with its own revenue/proof gate (e.g. 1.0 = public-tier 2-node discovery, NO confidential grants / no revocation authority / no SpiceDB replication → proves the federation seam cheaply).
20. **(C1 residual)** Ensure built Phase-0 carries one federation-flavored differentiator (cross-institution public-tier expert discovery on already-public OpenAlex — zero confidentiality machinery) reflecting D2's decomposition, so first revenue is not pure commodity secure-RAG.
21. **(C4)** State the kill-gate floor ASP at ≥2–3× modeled per-tenant COGS (per D7); model target logos × ACV vs monthly burn with a per-cell gross-margin floor.
22. **(C23)** Add a real monthly-burn model: 5 FTE loaded + fractional GRC/counsel + infra COGS + tooling + a discrete audit-fees line; overlay first-dollar and first-recurring-revenue dates. Sequence SOC2 Type I first; validate "sandbox pilot avoids full security review" in writing with a named buyer's CISO/contracts officer as a Week-1 kill-gate sub-condition.
23. **(C2/C23 race-window)** Reconcile the "lock ≥2 consortia under multi-year contracts" defensibility thesis against the real timeline — either compress (sign multi-year framework LOIs in Phase 0-1) or replace the race-window with the D1 network-effect lock-in (switching cost compounding with N nodes + accumulated sharing grants) as the stated moat.
24. **(C5/C7 hygiene)** Validate `Vault` key isolation: default to **cloud KMS per-tenant keys** (managed, no namespaces, no unseal toil); reserve self-run Vault for sovereign/on-prem. Do NOT assume Vault Community namespaces (Enterprise-only).
25. **(C7 hygiene)** Validate the SpiceDB cell-local replication / ZedToken→local-revision translation against SpiceDB's actual primitives in a Week-1 spike; treat as a go/no-go architectural fork (it is likely a bespoke build), not a free "configuration choice." (D5 reduces but does not eliminate the need for grant-tuple availability at the owning node.)

### E. ACCEPTED-TRADEOFF — must be explicitly documented
26. **(C12)** Document per-deployment-model: managed-cell = vendor-operated isolation + per-tenant keys + egress PEP, vendor is a *processor with technical+contractual controls*, NOT operator-zero-trust until TEE/HYOK sovereign. State plaintext-at-use exposure in cell memory honestly (mitigate via disabled swap/core-dumps, ephemeral DEK caching, TEE upgrade path). Relabel BYO-endpoint no-retention as a **contractual** control with a TEE-remote-attestation upgrade path.
27. **(C13)** Document separate SLOs for index-path vs brokered-path; explicit brokered-drill-down degraded mode ("owning node unreachable → publishable metadata only, detail unavailable"); state the honest **compound** availability of confidential cross-institution drill-down under correlated control-plane degradation (token-mint + authority + watermark feed share a failure domain).
28. **(C14)** Document the residual worst-case cross-region latency table (US subject → EU confidential data → generated answer) reconciled against the Q&A SLO — even after D5 removes the consensus hop.

**Key meta-finding:** D5 (no global hot-path consensus; owning node sole local authority) and D6 (confidential never in shared index; pre-write red-team gate) together resolve the largest and most-recurring critical clusters (C6, C7, C8) — these dominated all 4 rounds and were non-convergent precisely because the prior plan kept a global linearizable revocation authority and published confidential-derived signals. D4 (single PEP) resolves the entire "pluggable module impossibility" maintainability cluster. The remaining real work is concentrated in **scope-honesty/phasing (C22), burn-model realism (C23), and ~8 enforcement-detail gaps (C15–C20) that the locked decisions enable but do not themselves specify.**

Source file: `/home/anurag/codebase/tiger_research_buddy/plans/_findings.json`