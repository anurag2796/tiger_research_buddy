{
  "keep": 21,
  "drop": 8,
  "defer": 5,
  "contradictions": [
    "LEAKED AUTHORING MONOLOGUE in final-plan-v2.md lines 8-10: the canonical plan opens with the author's private planning narration ('This is a plan-authoring task, not a code task. I have the full current plan and the in-scope findings. I'll produce the strengthened, complete plan directly - no tools needed... I'll resolve each in the relevant section, keep all 19 sections at full depth, and preserve D1-D7.'). This is process meta-text accidentally committed into the authoritative deliverable and must not be carried into the new plan.",
    "TWO DIFFERENT '11 HIGH' LISTS that are conflated as if identical. (a) final-plan-v2.md line 3 + convergence-report.md describe the '11 high-severity refinement items' from the adversarial verification of the FEDERATED v2 plan (durability, epoch source, central-index PEP, derivative-store crypto, pooled isolation, projection field-schema, D7 reconciliation, expansion procurement, defensibility, COGS table, sandbox-vs-steady-state COGS). (b) phase0/00-index.md \u00a7'Where the 11 HIGH Refinement Items Are Addressed' lists a DIFFERENT, build-oriented 11 (actually tagged as 13 entries collapsed to 11 - see line 122/125: items are numbered 1-13). The two lists overlap in spirit but are NOT the same set and use different numbering; 00-index.md item #12 (share-correctness) and #13 (generated-draft) have no clean 1:1 mapping to the convergence list, and the convergence GTM/COGS/defensibility highs (#7-#11 there) are largely dropped or relabeled in the Phase-0 set. A reader is told 'the 11 HIGH items' as if one canonical list exists; there are two.",
    "00-index.md COUNT INCONSISTENCY (self-acknowledged but still confusing): the table tags 13 distinct highs_addressed entries (numbered 1-13 in the 'Where the 11 HIGH...' table, lines 111-123) but the section is titled '11 HIGH' and the prose (line 125) explains they 'collapse to 11' by grouping 0a's two economic items and 0g's two items. So a table titled '11' contains 13 numbered rows. Carry the collapse logic explicitly or renumber; do not present 13 rows under an '11' heading.",
    "COGS TABLE-B ARITHMETIC does not reconcile to its own line items (convergence-report.md verify-2, lines 49-51): Table B's six line items in final-plan-v2.md \u00a716.1 sum to $4.5k/mo=$54k/yr (shared-GPU) and $6.5k/mo=$78k/yr (Sovereign), but the table STATES totals of '~$3.0-3.6k/mo (~$36-43k/yr)' and '~$5.5-6k/mo (~$66-72k/yr)'. Re-summing \u00a716.1 Table B shared-GPU rows: $1.0+$2.0+$0.4+$0.3+$0.6+$0.2 = $4.5k/mo, which is $54k/yr, NOT the stated $36-43k/yr. Consequences the convergence report derives: the claimed '2.8-3.3x COGS' headroom is actually ~2.22x ($120k/$54k); the '>=60% gross-margin gate' FAILS at ~55% (60% needs COGS<=$48k, true is $54k); Sovereign '>=$150k clears 2x' is false (2x*$78k=$156k>$150k). This is moot for the new project (economics dropped) but is a live internal contradiction in the existing plan, in the very section that claims to have fixed it (Finding 10).",
    "TECH-STACK TABLE CONTRADICTS CONVENTIONS.md on two pinned decisions. (a) ABAC engine: final-plan-v2.md \u00a77.2 and \u00a712 list ABAC as 'Cedar (primary), OPA/Rego (fallback)', but CONVENTIONS.md \u00a73 declares 'ABAC = OPA' is authoritative and 'Any doc that LOCKS Cedar as the ABAC engine is WRONG.' The canonical plan and the canonical conventions disagree on the primary ABAC engine. (b) Python baseline: final-plan-v2.md \u00a712 lists 'Python 3.12 / FastAPI' and \u00a74.1/CLAUDE-style refs use 3.12, but CONVENTIONS.md \u00a74 declares 'Python 3.11+' authoritative and says 'DELETE any claim that the kernel pins 3.12 - it is false.' Plan body says 3.12; conventions say 3.11+. These must be reconciled (CONVENTIONS wins per its own \u00a710 precedence, but the plan body was never updated).",
    "PHANTOM DECISION LABELS in the plan corpus that CONVENTIONS.md \u00a75 had to explicitly outlaw: 'D-AI' and 'D9' are referenced in sub-plan/kernel docs (CONVENTIONS \u00a75 documents fixing them) even though only D1-D7 exist. This is a real inconsistency between the locked-decision set (00-decisions.md, D1-D7) and downstream docs that invented D-AI/D9; the new plan must use a single, complete decision set with no phantom labels.",
    "CONVENTIONS.md \u00a78 records that 0i (the retrieval sub-plan, ~line 2129) uses the deprecated/destructive Qdrant 'recreate_collection' instead of create-if-absent, and that 0h pins 'dagster>=1.7,<2' while the canonical pin is '>=1.8,<2'. These are unreconciled drift between sub-plans and the canonical conventions - flagged but (per the doc) not yet fixed in the source sub-plans.",
    "SHARE-CORRECTNESS vs REVOKE-CORRECTNESS ASYMMETRY (convergence-report.md verify-2 finding, \u00a74.3a/\u00a710.3d): the plan makes revocation bounded-stale-correct but governs the additive direction (publishing/widening discoverability_scope) only with a liveness SLO (share->discoverable <=30s), with no correctness gate preventing a projection from being emitted ABOVE its intended scope during a version-skew/propagation window. v2 claims to address this (0j 'no-wider-than-committed-scope property test') but the body \u00a710.3 still states it as a liveness SLO, not a correctness invariant - an unreconciled gap between the convergence fix and the plan body. (Federation-only; deferred for the new project, but a live inconsistency in the existing plan.)",
    "INTERNAL TENSION on whether the data-access broker holds raw-store credentials to MODULE-OWNED schemas: \u00a74.2 says the broker is 'the ONLY holder of raw-store credentials' and projects results for modules, while \u00a75.2 says each module 'owns its data; no other module reads its tables.' The convergence report (verify-2, lines 33-35) flags this as the load-bearing modularity contradiction (broker either reaches into every module's schema = re-centralized coupling, or each module fronts its own broker = 'single chokepoint' is imprecise). The plan's resolution (broker holds creds ONLY for the shared confidential-artifact/classification tables, NOT module-owned schemas) is stated in the convergence fix and Phase-0 HIGH #5, but \u00a74.2's 'ONLY holder of raw-store credentials' wording was never softened to match - so the canonical body still reads as the god-object version."
  ]
}